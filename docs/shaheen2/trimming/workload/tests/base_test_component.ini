# Default test component configuration.
# Lines beginning with '#' are comments; blank lines are ignored

[defaults]
work_root: ~/workload_test_suite/base_test_component

[base_test_component]
setup_script_source_dir:
setup_script:
setup_script_parameters:

test_script_source_dir:
test_script:
test_script_parameters:

num_concurrent_test_copies:

[alps]
#default aprun settings 
job_launcher:  

